### Высокоуровневое описание — «Фотоссылка бригады»

*(cрок действия ссылки — 1 месяц, с полным журналированием действий)*

---

#### 1. Назначение механизма

Данный механизм предоставляет членам монтажной бригады защищённую единую ссылку, по которой они могут в полевых условиях оперативно загрузить фотографии выполненных работ, не проходя полноценную авторизацию в основной системе SCAC. Система надёжно идентифицирует личность загрузившего по корпоративному адресу e‑mail и автоматически относит изображения к соответствующему проекту, фиксируя событие в официальной истории проекта.

---

#### 2. Жизненный цикл ссылки

1. **Генерация**
   При назначении бригады проекту сервер создаёт уникальный токен `crew_upload_token` и формирует адрес вида

   ```
   https://scac.app/upload/<ID_проекта>/<Токен>
   ```

   Токен криптографически стойкий, срок действия — ровно **30 суток** со дня создания.

2. **Распространение**
   Ссылка автоматически встраивается в описание калendarного события Google Calendar, а также доступна Project Leiter’у для ручной отправки.

3. **Активация**
   Работник открывает ссылку, вводит свой корпоративный e‑mail. Сервер сверяет e‑mail с таблицей `crew_members`. При совпадении отображается форма загрузки.

4. **Загрузка и верификация**
   После приёма файлов сервер:

   * сохраняет изображения в объектном хранилище (категория *report\_photo*);
   * создаёт записи в `project_files`;
   * фиксирует подробную запись в `project_history`.

5. **Историческая запись**
   Формат сообщения:

   ```
   [ДАТА, ВРЕМЯ] • Фото‑отчёт: добавлено 4 файла
   Пользователь: Иван Петров (ivan.petrov@firma.de)
   ```

   Запись не изменяется и хранится бессрочно.

6. **Истечение срока**
   По прошествии 30 дней токен помечается недействительным; переход по ссылке выводит уведомление «Срок действия ссылки истёк».

---

#### 3. Безопасность и контроль

| Аспект             | Реализация                                                            |
| ------------------ | --------------------------------------------------------------------- |
| **Идентификация**  | Только адреса, занесённые в `crew_members` текущей бригады            |
| **Токен**          | UUID v4, хранится в `projects.crew_upload_token` + `expiry`           |
| **Шифрование**     | Передача файлов исключительно по HTTPS (TLS 1.3)                      |
| **Ограничения**    | JPEG/PNG ≤ 10 МБ, не более 20 файлов за сессию                        |
| **Rate limit**     | 10 POST‑запросов в минуту с одного IP                                 |
| **Журналирование** | Каждое добавление фиксируется в `project_history` с имённым указанием |

---

#### 4. Пользовательский опыт

* **Поле «E‑mail»** с мгновенной валидацией.
* Зона **Drag‑&‑Drop** + кнопка «Выбрать файл».
* Превью миниатюр, счётчик файлов.
* По завершении — экран‑подтверждение «Спасибо, фото успешно отправлены».

---

#### 5. Интеграция с календарём

Описание мероприятия Google Calendar дополняется строкой:

```
📸 Фото‑отчёт бригады:
https://scac.app/upload/42/9f8b‑...‑4d1a
```

Таким образом, монтажники видят ссылку непосредственно в расписании выезда.

---

**Итог:** механизм обеспечивает безопасный, контролируемый и удобный канал передачи фотодоказательств выполненных работ, при этом каждая загрузка автоматически отражается в летописи проекта, сохраняет юридическую значимость доказательств и повышает прозрачность взаимодействия между бригадой и Project Leiter’ом.
